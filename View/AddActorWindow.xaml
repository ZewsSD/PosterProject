<Window x:Class="Poster.View.AddActorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Poster.ViewModel"
        xmlns:local="clr-namespace:Poster.View"
        mc:Ignorable="d"
        Title="AddActorWindow"  MaxHeight="370" MaxWidth="250" MinHeight="350" MinWidth="230">
    <Window.DataContext>
        <vm:AddActorViewModel/>
    </Window.DataContext>
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Имя" FontSize="20" Margin="5"/>
            <TextBox Margin="5" />
            <TextBlock Text="Фамилия" FontSize="20" Margin="5"/>
            <TextBox  Margin="5" />
            <TextBlock Text="Отчество" FontSize="20" Margin="5"/>
            <TextBox  Margin="5" />
            <Button Content="Добавление" MinHeight="50" Margin="15"/>
        </StackPanel>
    </Grid>
</Window>
